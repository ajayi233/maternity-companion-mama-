---
# Ansible Galaxy Requirements for MAMA App
collections:
  - name: amazon.aws
    version: ">=5.0.0"
  - name: community.docker
    version: ">=3.0.0"
  - name: ansible.posix
    version: ">=1.0.0"
